#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: c is an integer
 *
 * Return: Always 0
*/

int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
