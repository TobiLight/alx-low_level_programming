#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: c is an integer
 *
 * Return: 1 if uppercase, 0 if not uppercase
*/

int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
		return (1);

	return (0);
}
