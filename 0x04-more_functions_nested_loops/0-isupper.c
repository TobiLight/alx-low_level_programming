#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: c is a character
 *
 * Return: Always 0
*/

int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
	{
		return (1);
	}

	return (0);
}
